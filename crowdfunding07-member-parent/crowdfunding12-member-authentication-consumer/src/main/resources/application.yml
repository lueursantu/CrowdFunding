server:
  port: 4000
spring:
  application:
    name: crowd-authentication
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
eureka:
  client:
    service-url:
      defaultZone: http://localhost:1000/eureka